# Personal Library Tracker - خطوات النشر على Netlify

هذا الملف يحتوي على تعليمات مفصلة لنشر مشروع Personal Library Tracker على منصة Netlify.

## المقدمة

مشروعك هو تطبيق كامل (Full-Stack) يتكون من:
- واجهة أمامية (Frontend) مبنية بـ React
- خادم خلفي (Backend) مبني بـ Express.js

لنشر هذا النوع من المشاريع على Netlify، نحتاج إلى إعدادات خاصة لأن Netlify يعمل بشكل أفضل مع تطبيقات الواجهة الأمامية فقط.

## الخطوات التفصيلية للنشر

### 1. تسجيل الدخول إلى حسابك على Netlify

- افتح موقع [Netlify](https://app.netlify.com/)
- قم بتسجيل الدخول باستخدام حسابك (GitHub أو البريد الإلكتروني)

### 2. ربط المشروع بـ GitHub

- بعد تسجيل الدخول، انقر على زر "Import from Git"
- اختر GitHub كمصدر للمشروع
- اختر المستودع (Repository) الذي يحتوي على مشروعك

### 3. إعداد خيارات النشر

عند إعداد خيارات النشر، استخدم الإعدادات التالية:

- **Owner**: (اسم حسابك على GitHub)
- **Branch**: main (أو الفرع الرئيسي الذي تستخدمه)
- **Base directory**: PersonalLibraryTracker (تأكد من أن هذا هو المجلد الذي يحتوي على ملف package.json)
- **Build command**: npm run build
- **Publish directory**: dist/public

### 4. إعدادات متقدمة (مهم جداً)

قبل النقر على زر "Deploy site"، انقر على "Show advanced" وأضف المتغيرات البيئية التالية:

- **Key**: NODE_VERSION
- **Value**: 18 (أو أحدث)

### 5. بدء عملية النشر

- انقر على زر "Deploy site" لبدء عملية النشر
- انتظر حتى تكتمل عملية البناء والنشر (قد تستغرق بضع دقائق)

### 6. التحقق من النشر

- بعد اكتمال عملية النشر، ستظهر رسالة "Site is live"
- انقر على الرابط المقدم للتحقق من أن موقعك يعمل بشكل صحيح

## حل المشكلات الشائعة

### مشكلة: الصفحة غير موجودة (404)

إذا ظهرت رسالة "Page not found" بعد النشر:

1. انتقل إلى إعدادات موقعك على Netlify
2. انقر على "Deploys" ثم "Deploy settings"
3. تأكد من أن إعدادات البناء صحيحة (كما هو موضح في الخطوة 3)
4. أعد النشر مرة أخرى

### مشكلة: فشل عملية البناء

إذا فشلت عملية البناء:

1. انقر على "Deploys" ثم على آخر عملية نشر
2. انقر على "Deploy log" لمعرفة سبب الفشل
3. تأكد من وجود ملف netlify.toml في المجلد الرئيسي للمشروع
4. تأكد من تثبيت جميع الحزم المطلوبة

## ملاحظات هامة

- تم إضافة ملف `netlify.toml` إلى مشروعك لتكوين عملية النشر بشكل صحيح
- تم إضافة مجلد `netlify/functions` لدعم الخادم الخلفي (Backend)
- تأكد من أن فرع GitHub الذي تستخدمه يحتوي على هذه الملفات الجديدة

إذا واجهت أي مشكلة أخرى، يمكنك الرجوع إلى [وثائق Netlify](https://docs.netlify.com/) أو طلب المساعدة.
